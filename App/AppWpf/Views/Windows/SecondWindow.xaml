<Window x:Class="AppWpf.Views.Windows.SecondWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppWpf.Views.Windows"
        mc:Ignorable="d"
        DataContext="{Binding SecondWindowModel, Source={StaticResource Locator}}"
        Title="{Binding Title}" 
        Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <DataGrid  Grid.Row="1"   VerticalAlignment="Top"
                   Margin="5"
          ItemsSource="{Binding AutoModels}"
          AutoGenerateColumns="False" CanUserResizeColumns="True" 
          ScrollViewer.CanContentScroll="True" 
          ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Водитель" Binding="{Binding Path=Driver}"/>
                    <DataGridTextColumn Header="Машина" Binding="{Binding Path=Car}"/>
                    <DataGridTextColumn Header="Время" Binding="{Binding Path=Date,StringFormat=dd.MM.yyyy HH:mm:ss}"/>
                </DataGrid.Columns>
            </DataGrid>
    </Grid>
</Window>
